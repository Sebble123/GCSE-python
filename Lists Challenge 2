cars =[]

cont = True
while cont == True:
    car = input("input a car brand or press x to exit ")
    if car == "x":
        print(cars)
        print("exiting")
        break
    else:
        cars.append(car)
